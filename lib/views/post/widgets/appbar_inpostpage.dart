import 'package:flutter/material.dart';

PreferredSizeWidget appBarInPostInputPage() {
  return AppBar(
    backgroundColor: Colors.transparent,
    elevation: 0,
    title: const Text(
      'Add Post',
    ),
  );
}
