import 'package:flutter/material.dart';

class ViewTaggedFriends extends StatelessWidget {
  const ViewTaggedFriends({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tagged Friends'),
      ),
    );
  }
}
